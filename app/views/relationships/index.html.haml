%section.people
  %header
    %h2 People I Follow
  %table.table
    %thead
      %tr
        %th(width="30%")
        %th(width="20%") Videos in Queue
        %th(width="20%") Followers
        %th(width="30%") Unfollow
    %tbody
      - @relationships.each do |following|
        %tr
          %td
            %img(src="http://www.gravatar.com/avatar/#{Digest::MD5.hexdigest('knwang@gmail.com'.downcase)}?s=40")
            = link_to "#{following.leader.full_name}"
          %td.extra-padding #{following.leader.queue_items.count}
          %td.extra-padding #{following.leader.is_leading.count}
          %td.extra-padding
            = link_to following,  method: :delete do
              %i.glyphicon.glyphicon-remove
